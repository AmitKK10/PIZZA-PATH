/*const handleBaseChange = (newBase) => {
    setBase(newBase);
    if (newBase === "Gluten-Free") {
      setPrice(price + 50); // Add Rs. 50 for gluten-free base
    } else {
      setPrice(price - 50); // Reset to the regular price
    }
  };
  
  const handleSauceChange = (newSauce) => {
    setSauce(newSauce);
    // You can add additional price logic for different sauces if necessary
  };
  
  */